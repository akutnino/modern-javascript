// Primitive Type
// String
const firstName = 'John';

// Number
const age = 30;
const temp = 98.9;

// Boolean
const hasKids = true;

// Null
const aptNumber = null;

// Undefined
let sample = undefined;

// Symbol
const id = Symbol('id');

// BigInt
const n = 90045876321257n;

// Reference Type
// Array
const array = [100, 'hello', true, null, undefined];

// Object
const person = {
  firstName: 'Brad',
};

// Function
function sampleFunc() {
  return 'Hello';
}

const output = sampleFunc();

console.log(output, typeof output);
